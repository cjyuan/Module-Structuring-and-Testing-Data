# Files with `.js` (not `.test.js`) extension
These files contain sample solution and code to test the implemented function.


# Files with `.test.js` extension
These are the revised version of the .js file with assertion written in Jest syntax.
